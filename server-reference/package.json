{
  "name": "monshaat-consultation-backend",
  "version": "1.0.0",
  "description": "Reference proxy that holds the NocoDB token and serves the consultation site.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "engines": { "node": ">=20.6" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
